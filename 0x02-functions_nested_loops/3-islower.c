#include "main.h"

/**
 * _islower -> checks if the character c is in lowercase
 * @c: a character argument
 * Return: 1 if true, 0 if false
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
