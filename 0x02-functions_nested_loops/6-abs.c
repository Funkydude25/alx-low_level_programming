#include "main.h"

/**
 * _abs -> print an absolute integer
 * @r: a number argument
 * Return: absolute value of a number
 */

int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
}
