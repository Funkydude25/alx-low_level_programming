#include "main.h"

/**
 * add -> two integers together
 * @i: the first variable
 * @j: the second variable
 * Return: return the result
 */

int add(int i, int j)
{
	int n;
	 n = i + j;
	return (n);
}
